# Mirror of the core dependencies declared in pyproject.toml.
# Keep in sync with `project.dependencies`. Framework-specific extras
# (langgraph, crewai, autogen) are not installed by default because the
# benchmark can execute against the built-in mock LLM with zero extras.
openai==2.38.0
pydantic==2.13.4
typing-extensions==4.15.0
rich==15.0.0
httpx==0.28.1

# Development tooling (matches project.optional-dependencies.dev)
pytest==9.0.3
pytest-cov==7.1.0
ruff==0.15.15
black==26.5.1
mypy==2.1.0
